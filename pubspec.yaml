name: dialect
description: AI-native localization toolkit for Flutter-led teams.
version: 0.1.0-dev
repository: https://github.com/chaucao/dialect

environment:
  sdk: ^3.4.0

executables:
  dialect: dialect

dependencies:
  args: ^2.7.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
